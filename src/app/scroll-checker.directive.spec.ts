import { ScrollCheckerDirective } from './scroll-checker.directive';

describe('ScrollCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
